import 'package:flutter/material.dart';

class IpGeoPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Welcome to IP Geo Location Page!'),
      ),
    );
  }
}
